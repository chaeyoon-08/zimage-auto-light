# 핵심 라이브러리 (torch / diffusers 는 Dockerfile에서 별도 설치)
accelerate>=0.34.0
safetensors>=0.4.2
transformers>=4.44.0
pillow>=10.4.0
uvicorn[standard]>=0.30.0
fastapi>=0.115.0
sdnq>=0.1.2
# 모델을 빌드 단계에서 받기 위한 CLI (hf download) — v0.34+ 에서 hf CLI 제공
huggingface_hub>=0.34.0